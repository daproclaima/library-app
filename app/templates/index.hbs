<h1>{{headerMessage}}</h1>
<div class="jumbotron text-center">

  <h1>{{t "label.launch_date"}}</h1>

  <br/>
  <br/>

  <p>{{t "label.fill_infos"}}</p>

  <div class="form-horizontal form-group form-group-lg row">

    <div class="col-xs-10 col-xs-offset-1 col-sm-6 col-sm-offset-1 col-md-5 col-md-offset-2 form-object-y">
      {{input type="text" value=firstName class="form-control" placeholder="Please type your first name."}}
    </div>

    <div class="col-xs-10 col-xs-offset-1 col-sm-6 col-sm-offset-1 col-md-5 col-md-offset-2 form-object-y">
      {{input type="text" value=lastName class="form-control" placeholder="Please type your last name." }}
    </div>

    <div class="col-xs-10 col-xs-offset-1 col-sm-6 col-sm-offset-1 col-md-5 col-md-offset-2 form-object-y">
      {{input type="email" value=emailAddress class="form-control"
              placeholder="Please type your e-mail address."
              autofocus="autofocus"}}
    </div>

    <div class="col-xs-10 col-xs-offset-1 col-sm-6 col-sm-offset-1 col-md-5 col-md-offset-2 form-object-y">
      {{input type="date" value=birthday class="form-control" placeholder="Please type birthday."}}
    </div>


    <div class="col-xs-10 col-xs-offset-1 col-sm-6 col-sm-offset-1 col-md-5 col-md-offset-2 form-object-y">
      <h6 class="card-header">Gender</h6>
      <div class="form-check">
        {{input type="radio" value=male class="form-check-input" name="gender" label="Male" id="radioMale"}}
        <label class="form-check-label" for="radioMale">Male</label>

        {{input type="radio" value=female class="form-check-input" name="gender" label="Female"
                id="radioFemale"}}
        <label class="form-check-label" for="radioFemale">Female</label>
      </div>
    </div>

    <div class="col-xs-10 col-xs-offset-0 col-sm-offset-0 col-sm-4 col-md-10 col-md-offset-0">
      <button class="btn btn-primary btn-lg btn-block" disabled={{isDisabled}} {{action "saveInvitation"}}>
        {{t "link.request_invit"}}
      </button>
    </div>

  </div>

  {{#if responseMessage}}
    <div class="alert alert-success col-10 col-offset-1">{{responseMessage}}</div>
  {{/if}}

  <br/>
  <br/>
</div>
